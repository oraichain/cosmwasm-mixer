{
  "scripts": {
    "test": "node test.js",
    "build-wasm": "cd wasm/mixer_js && wasm-pack build --target nodejs",
    "build": "bash build_release.sh contracts/mixer"
  },
  "dependencies": {
    "@oraichain/cosmosjs": "^0.1.7",
    "dotenv": "^16.0.1"
  },
  "devDependencies": {
    "@types/node": "^18.7.12",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
